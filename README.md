liten quote netside bygd med next.js fetcher qoutes fra hvilket api du vil så lenge den har keyen text || quote for å vise quoten

går gjennom et api på next server for å verifisere at apis faktisk er fungerende ble ikke helt ferdig kan legge til mye mer som sortering søking osv 

jeg kom ikke på noe bedre for så kort tid kunne kanskje heller ha fokusert litt på annet språk som Rust, eller c++ bare for å teste litt

aldri gjør som jeg har gjort i dette api'et kan fetche fra lokale ip-er er ikke sikkert ville bare teste noe med api params i next
# upStart
clone project


```shell
npm i

npm run dev
```